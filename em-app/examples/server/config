manager_url="https://em.fortanix.com"
username="user@example.com"
password="password"
account_name="homelab"
app_name="testapp"
isvprodid=0
isvsvn=0

# Update the domain here and in call to 'get_fortanix_em_certificate' in src/main.rs before running.
domain="localhost"

# Time to wait between requests in seconds
rate_limit=0.5

# use minimal CURL library instead of em-cli for script purposes
curl_override="n"
curl_opts=""